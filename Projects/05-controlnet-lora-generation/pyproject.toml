[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "controlnet-lora-generation"
version = "0.1.0"
description = "Auditable ControlNet and LoRA project utilities"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
